/* Generated By:JJTree: Do not edit this line. SQLLetClause.java Version 4.3 */
/* JavaCCOptions:MULTI=true,NODE_USES_PARSER=false,VISITOR=true,TRACK_TOKENS=true,NODE_PREFIX=O,NODE_EXTENDS=,NODE_FACTORY=,SUPPORT_CLASS_VISIBILITY_PUBLIC=true */
package com.jetbrains.youtrack.db.internal.core.sql.parser;

import com.jetbrains.youtrack.db.internal.core.db.DatabaseSessionInternal;
import com.jetbrains.youtrack.db.api.query.Result;
import com.jetbrains.youtrack.db.internal.core.sql.executor.ResultInternal;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.stream.Collectors;

public class SQLLetClause extends SimpleNode {

  protected List<SQLLetItem> items = new ArrayList<SQLLetItem>();

  public SQLLetClause(int id) {
    super(id);
  }

  public SQLLetClause(YouTrackDBSql p, int id) {
    super(p, id);
  }

  public void toString(Map<Object, Object> params, StringBuilder builder) {
    builder.append("LET ");
    var first = true;
    for (var item : items) {
      if (!first) {
        builder.append(", ");
      }
      item.toString(params, builder);
      first = false;
    }
  }

  public void toGenericStatement(StringBuilder builder) {
    builder.append("LET ");
    var first = true;
    for (var item : items) {
      if (!first) {
        builder.append(", ");
      }
      item.toGenericStatement(builder);
      first = false;
    }
  }

  public void addItem(SQLLetItem item) {
    this.items.add(item);
  }

  public SQLLetClause copy() {
    var result = new SQLLetClause(-1);
    result.items = items.stream().map(x -> x.copy()).collect(Collectors.toList());
    return result;
  }

  public List<SQLLetItem> getItems() {
    return items;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }

    var that = (SQLLetClause) o;

    return Objects.equals(items, that.items);
  }

  @Override
  public int hashCode() {
    return items != null ? items.hashCode() : 0;
  }

  public boolean refersToParent() {
    for (var item : items) {
      if (item.refersToParent()) {
        return true;
      }
    }
    return false;
  }

  public void extractSubQueries(SubQueryCollector collector) {
    for (var item : items) {
      item.extractSubQueries(collector);
    }
  }

  public Result serialize(DatabaseSessionInternal db) {
    var result = new ResultInternal(db);
    if (items != null) {
      result.setProperty(
          "items",
          items.stream().map(oLetItem -> oLetItem.serialize(db)).collect(Collectors.toList()));
    }
    return result;
  }

  public void deserialize(Result fromResult) {

    if (fromResult.getProperty("items") != null) {
      List<Result> ser = fromResult.getProperty("items");
      items = new ArrayList<>();
      for (var r : ser) {
        var exp = new SQLLetItem(-1);
        exp.deserialize(r);
        items.add(exp);
      }
    }
  }

  public boolean isCacheable(DatabaseSessionInternal session) {
    for (var item : items) {
      if (!item.isCacheable(session)) {
        return false;
      }
    }
    return true;
  }
}

/* JavaCC - OriginalChecksum=201a864b5ed7f1fbe0533843a7acd03d (do not edit this line) */
